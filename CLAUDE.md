@AGENTS.md

Nested `AGENTS.md` files may exist in subdirectories. When working in or beneath
a directory that contains an `AGENTS.md`, read that file as well. Guidance from a
nested `AGENTS.md` applies to its directory and descendants, and takes precedence
over higher-level `AGENTS.md` files on conflict.
