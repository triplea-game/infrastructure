# Managed by Ansible - do not edit manually. Secrets sourced from ansible-vault.
# Every secret belongs here rather than in 'docker-compose.yml', which the role
# deploys world-readable (0644); this file is 0600 and compose interpolates it.
DB_PASSWORD={{ database_password }}
GITHUB_API_TOKEN={{ github_api_token }}
